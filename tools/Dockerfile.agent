FROM python:3.11-slim
RUN apt-get update && apt-get install -y curl bash ca-certificates && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY . /app
RUN chmod +x /app/jarvas-lite-run.sh || true
CMD ["/app/jarvas-lite-run.sh","agent"]
